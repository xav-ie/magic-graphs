import { watch } from 'vue'
import type { Node as GNode, Graph } from './useGraph';

// const defaultColorPalette = ['red', 'green', 'blue', 'yellow', 'purple', 'orange'];

// export const bfsNodeColorizer = (graph: Graph, colors = defaultColorPalette) => {

//   graph.

//   return (node: GNode) => {
//     return colors[node.id % colors.length];
//   }
// }