import type { GraphThemeName } from "@graph/themes";
import colors, { type Color } from "@utils/colors";

export const GToolbarHintTheme: Record<GraphThemeName, Color> = {
  light: colors.BLACK + "75",
  dark: colors.WHITE + "75",
  girl: colors.PINK_100,
};