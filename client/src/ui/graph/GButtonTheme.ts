import type { GraphThemeName } from "@graph/themes";
import colors, { type Color } from "@utils/colors";

export const GButtonTheme: Record<GraphThemeName, Color> = {
  light: colors.GRAY_800,
  dark: colors.GRAY_200,
  girl: colors.PINK_600,
};