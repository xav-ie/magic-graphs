import type { GraphSettings } from "@graph/settings";

/**
 * settings for graph sandbox useGraph instance
 */
export const SANDBOX_GRAPH_SETTINGS: Partial<GraphSettings> = {
  persistentStorageKey: 'graph-sandbox',
};